/**
 * Data transfer objects used by the controller layer.
 *
 * <p>
 * DTOs are generally implemented as Java records to provide concise, immutable
 * representations. Complex flat DTOs may use regular classes when record
 * constructors would become unwieldy and negatively affect readability.
 */
package sk.mkrajcovic.challenges.controller.dto;
